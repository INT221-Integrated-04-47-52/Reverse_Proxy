version: '3'
services:
 reverseproxy:
    container_name: Rproxy
    image: rproxy
    volumes:
            - ./nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
            - "80:80"
networks:
    default:
       external:
          name: network