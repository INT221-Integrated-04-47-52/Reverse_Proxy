version: '3'
services:
  Rproxy:
    container_name: Rproxy_con
    image: nginx
    volumes:
            - ./nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
            - "80:80"
networks:
    default:
       external:
          name: network